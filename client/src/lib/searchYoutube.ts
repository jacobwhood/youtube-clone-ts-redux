interface IOptionsObject {

};

interface ISearchResult {

};

const searchYoutube = (options: IOptionsObject): ISearchResult[] => {
  return new Array() as ISearchResult[];
};

export default searchYoutube;
